services:
  api:
    build:
      target: builder
    ports:
      - '${PORT:-3000}:3000'
    environment:
      NODE_ENV: '${NODE_ENV:-development}'
    volumes:
      - .:/app
      - /app/node_modules

    command: ['node', '--watch', '--watch-preserve-output', 'src/express.js']
