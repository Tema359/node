services:
  api:
    build:
      target: builder
    ports:
      - '${PORT:-3000}:3000'
    environment:
      NODE_ENV: '${NODE_ENV:-development}'
    volumes:
      - .:/app
      - /app/node_modules

    command: ['sh', '-c', 'npm run build:ioc && node dist/ioc/api.js']
